name: workflow1
on:
  push:
      branches:
          -main
jobs:
    job1:
        runs-on : ubuntu-lates
        steps:
          -name : checkout code
           uses : actions/checkout@v2
          -name : print message
           run : echo "Welcome to Avd data ops class"
