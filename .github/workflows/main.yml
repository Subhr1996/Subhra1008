name : workflow1
on:
  schedule:
  - cron : "*/2 * * * *"
jobs:
    job1:
      runs-on : ubuntu-lates
      steps:
      - name: checkout code
        uses: actions/checkout@v2
      - name: print message
        run: echo "Welcome to Avd data ops class"
 